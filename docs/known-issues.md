# Known Issues

## Pending

- Simplify the Trae provider model list. Remove human-curated labels and aliases such as `default`, `fast`, `balanced`, `strong`, and `coding`; expose only the actual Trae model names users can select directly.
